# IMPORTANT: wrap the master key in single quotes — it contains $ chars
# that the dotenv parser would otherwise treat as variable references.
JSONBIN_MASTER_KEY='your_jsonbin_master_key_here'
JSONBIN_BIN_ID=will_be_set_after_first_create_via_init_script
